---
name: example-cross-project-fact
description: "Example of a cross-project learned fact — delete or replace."
metadata:
  node_type: memory
  type: reference
---

This is the format for a **cross-project** learned fact (one fact per file). Project-specific facts
live under `projects/<name>/memory/` instead (Claude Code writes those automatically).

Link related notes with [[wikilinks]]. Set `metadata.type` to one of:
`user` (about Rakshit), `feedback` (how agents should work), `project`, or `reference`.

_Delete this file once you have real facts._
